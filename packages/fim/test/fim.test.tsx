import { Fim } from '../src/Fim'

test.only('required fail', () => {
  const value = ''
  const options: any = {}
  const msg = 'required'
  // expect(required(value, msg, options)).toEqual(msg + 'x')
})
